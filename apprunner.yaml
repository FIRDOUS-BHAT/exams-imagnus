version: 1.0
runtime: python3
build:
  commands:
    pre_build:
      - python -m venv venv
      - . venv/bin/activate
      - pip install -r requirements.txt
run:
  command: . venv/bin/activate && python main.py
  env_variables:
    - name: PORT
      value: "8000"
  network:
    port: 8000
