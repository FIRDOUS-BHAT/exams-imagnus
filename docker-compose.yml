version: "3"

services:
  imagnus:
    build: .
    volumes:
      - .:/code
    image: imagnusimage
    command:  uvicorn main:app --reload --workers 1 --host 0.0.0.0 --port 8000
    ports:
      - 8000:8000
#     networks:
#       - imagnus_network

# networks:
#   imagnus_network:
#     driver: bridge