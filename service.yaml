# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
  namespace: imagnus-namespace
  labels:
    app: fastapi
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: fastapi
